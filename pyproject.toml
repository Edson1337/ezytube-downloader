[project]
name = "yt-downloader"
version = "1.0.0"
description = "Desktop application for downloading YouTube videos"
requires-python = ">=3.10"
dependencies = [
    "yt-dlp>=2025.1.26",
    "pyinstaller>=6.0.0",
    "mutagen>=1.47.0",
]
